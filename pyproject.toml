[project]
name = "steves-cookbook"
version = "0.1.0"
description = "Tools for digitizing Steve's recipe collection"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.76.0",
    "mistralai>=1.10.0",
]
